(import "random.arkm")

(let in-range? (fun (x start end) (and (>= x start) (<= x end))))

(assert (in-range? (random) 0 32768) "random")
(assert (in-range? (rand10) 0 10) "rand10")

(print "Random tests done")
